#
# Sample configuration file for ISC dhcpd for Debian
#
# Attention: If /etc/ltsp/dhcpd.conf exists, that will be used as
# configuration file instead of this file.
#
#

# The ddns-updates-style parameter controls whether or not the server will
# attempt to do a DNS update when a lease is confirmed. We default to the
# behavior of the version 2 packages ('none', since DHCP v2 didn't
# have support for DDNS.)
ddns-update-style none;

# option definitions common to all supported networks...
one-lease-per-client true;
default-lease-time 86400;
max-lease-time 86400;

option option-128 code 128 = string;
option option-129 code 129 = text;

# If this DHCP server is the official DHCP server for the local
# network, the authoritative directive should be uncommented.
authoritative;

# Use this to send dhcp log messages to a different log file (you also
# have to hack syslog.conf to complete the redirection).
log-facility local7;

subnet 192.168.1.0 netmask 255.255.255.0 {

  option domain-name "home";
  option routers 192.168.1.1;
  option subnet-mask 255.255.255.0;
  option broadcast-address 192.168.1.255;
  option domain-name-servers 192.168.1.5;
  option ntp-servers 192.168.1.1;

  allow booting;
  allow bootp;
  if exists user-class and option user-class = "iPXE" {
    # IPXE config settings http://ipxe.org/cfg and an example
    # of implementing them http://ipxe.org/cfgtype/string
    filename "http://192.168.1.5/menu.ipxe";
  } else {
    filename "undionly.kpxe";
    next-server 192.168.1.5;
  }
 
  range 192.168.1.10 192.168.1.254;

  host collins {
    hardware ethernet 36:37:61:34:61:61;
    fixed-address 192.168.1.5;
  }

  host seagate {
    hardware ethernet 00:10:75:4E:5B:AE;
    fixed-address 192.168.1.3;
  }

  host plex {
    hardware ethernet 32:65:38:63:64:36;
    fixed-address 192.168.1.6;
  }

}
